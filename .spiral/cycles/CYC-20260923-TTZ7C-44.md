---
id: CYC-20260923-TTZ7C-44
---

# File-backed dataset runtime

Goal: replace shared-memory dataset transport and retention with file-backed
reads throughout live operation and administrative replay, preserving remote
behavior, disk formats, durable command ordering and recovery restrictions.

Branch: spiral/CYC-20260923-TTZ7C-44-file-backed, from master dbb0703.
Status: Active. Human authorization is retained in SRC-20260923-TTZ7C-45.
Continue the file-only roadmap direction; prioritize it ahead of rebuild under
the maintainer's explicit direction. Library readiness was established first.

Evaluate with existing durability/recovery tests, file-only worker messages,
ID lookup of unread records, sparse overlays, no-op commands, worker replacement
and shutdown, index/framing/integrity failures and a bounded-memory large-file
probe. Source changes must remain confined to disposable fixtures during tests.
Client version metadata/audit history and sandbox replacement remain later work.

## Evaluation

Implementation, including requested persisted-index loading, is complete at
`46fb22fe5c4fecbb23c3b8d3b59e5ff6e58e11c2`. EVD-20260923-TTZ7C-49
records 178 passing tests, clean focused lint, fresh-package installation, a
329 MB/48 MB-heap runtime probe and read-only curriculum evidence.
See [the integration guide](../../docs/file-data.md) for the source contract and
limits. Human acceptance remains pending; the cycle stays Active.

Final review added DEF-20260923-TTZ7C-50 and two reproducing tests. The correction
stops mutation after late lazy-read failures, including caught errors, and keeps
query-supplied flags from masquerading as parser failures. EVD-49 records the
full 142-test pass and the final 23-test response-format check.

The maintainer's example-folder verification and subsequent corrections are
retained in SRC-45. REQ-46/DES-47 now require direct trusted ID loading, reliable
final ID publication, errors for duplicate IDs and no legacy index compatibility.
Missing indexes reconstruct; validation/reconstruction can be explicitly
requested. EVD-49 records the 170-test final pass and direct-index memory probe.
The cycle remains Active pending acceptance of this corrected implementation.

The maintainer then confirmed offset trust and file hashes. Normal startup now
loads both indexes directly, including inspection; explicit validation or missing
offsets alone reconstruct framing. Existing integrity manifests cover final
standard indexes too. EVD-49 records 178 passing tests, clean lint, and the updated
large-file probe. The cycle remains Active pending human review.
