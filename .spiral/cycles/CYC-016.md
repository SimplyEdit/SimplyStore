---
id: CYC-016
---

# Cycle: Causal Provenance Graph Repair

Repository branch: `spiral/CYC-016-causal-provenance-repair`
Branch verified: verified on 2026-08-17 with `git branch --show-current`, branched from the CYC-015 tip because the required verification examples include the unmerged after-change/index characterization artifacts.

## Analyze

Project state / prior evaluation that makes this cycle relevant:

An independent review found that early baseline artifacts `SRC-001`, `UND-001`, `REQ-001`, and `DES-001` were introduced in the same Git commit. Because Spiral historical causal references must point to strict ancestor commits, the original machine-readable graph could not legally encode the intended `source -> understanding -> request -> design` chain. Later implementation artifacts therefore often trace to designs/evidence/cycles but cannot mechanically traverse all the way back to original human intent. The review also found a shortened Git hash in `EVD-008.ttl`.

Nearest important risk / uncertainty / desired movement:

Spiral's core claim is that a maintainer or AI can inspect code and recover why it exists, including original human intent and later surviving contributions. If the causal graph cannot answer that mechanically for SimplyStore's durability work, the process evidence is weaker than the prose suggests.

Relevant human direction / feedback:

The human explicitly requested a new cycle that repairs this prospectively, without rewriting or rebasing existing history and without changing SimplyStore runtime behavior.

Governing higher-level plan / direction:

This is a process/provenance repair governed by Spiral's causal validation and implementation-lineage rules, especially the distinction between current effective provenance and historical transition provenance.

Current position in that plan:

The project has adopted Spiral and accumulated implementation/evidence artifacts. This cycle repairs the causal graph so existing durability implementation concerns can be traversed to current effective intent while preserving separate implementation history.

## Plan

### Cycle goal

Repair the machine-readable Spiral causal graph so representative implementation concerns can traverse through design, request, understanding, and retained/referenced source using full strict-ancestor Git references, without changing SimplyStore runtime behavior.

### Why now / why this cycle boundary

The gap directly affects Spiral's "why does this code exist?" claim. The work is coherent as a process/provenance repair cycle and should not be mixed with product behavior changes.

### Plan continuity decision

Revise local process artifacts, not the durability/product plan. This cycle temporarily interrupts the product durability sequence to repair the provenance substrate required to trust future and existing Spiral evidence.

### Current starting evidence

- `SRC-001`, `UND-001`, `REQ-001`, and `DES-001` were introduced together, so their first versions could not legally reference each other as strict ancestors.
- Current `UND-001.ttl`, `REQ-001.ttl`, and `DES-001.ttl` do not encode the source/understanding/request/design chain.
- `EVD-008.ttl` contains short hash `c8e55be`.
- Spiral core documentation distinguishes current effective provenance from `sd:transforms` and `sd:changeCausedBy` history.

### Evaluation basis

- New versions of `SRC-001`, `UND-001`, `REQ-001`, and `DES-001` are created in separate semantic commits.
- Current machine-readable links permit traversal `implementation -> design -> request -> understanding -> source`.
- Historical/change links remain separate from current effective justification.
- `EVD-008.ttl` uses a full resolving 40-character Git hash.
- Verification mechanically parses Turtle, resolves full Git references, checks strict ancestry, checks for short hashes in provenance data, checks branch/cycle consistency, and demonstrates representative traversals.

### Likely work

- Add prospective repair notes to the relevant provenance artifacts.
- Add strict-ancestor `sd:interprets` / `sd:derivedFrom` links across `SRC-001`, `UND-001`, `REQ-001`, and `DES-001` in separate commits.
- Update current implementation effective provenance where needed so representative implementations can traverse to `DES-001` or `DES-002` and onward.
- Repair `EVD-008.ttl`.
- Add mechanical graph validation/traversal tooling and evidence.

### Explicit non-goals

- Change SimplyStore runtime/product behavior.
- Rewrite, amend, rebase, squash, or otherwise alter existing published history.
- Falsely claim the original implementation history had links that did not exist at the time.
- Collapse implementation lineage/history into current effective justification.
- Complete every possible provenance path in the repository if representative traversal exposes remaining lower-priority gaps.

### Pause / re-plan conditions

- Mechanical traversal requires changing runtime/product code.
- Existing ontology cannot express the current-effective versus historical distinction without misleading semantics.
- Full strict-ancestor repair of representative paths would require rewriting published commits.

## Act

Important artifacts / semantic commits produced:

- `36fc816a6357b57f99843359aeb4a2329677d6c1` opened `CYC-016` on `spiral/CYC-016-causal-provenance-repair`.
- `f2c3a597c12c2dbe6d7467582d5a3f4308acf02d` versioned `SRC-001` for prospective provenance repair.
- `154b682cd12890d31a9420e884ad078ed7ce1a8f` linked `UND-001` to the retained source.
- `6a9977eb22b0a610a82da5c52864e3bb72b164c9` linked `REQ-001` to the understanding.
- `85913e982e163c7b05a8e616c00e3f2014e775cc` linked `DES-001` to the request.
- `50155591f6e294be5fec3f93fa95406c11128e63` linked `DES-002` to the repaired request/design chain.
- `68c38971e11a1eb81a34e7f1da1eb6f18019a3c4` repaired representative implementation provenance projections while preserving historical lineage with `sd:transforms` and `sd:changeCausedBy`.
- `289858fa76013797c4f06b33cc4ebddae51f666d` repaired the short hash in `EVD-008.ttl`.
- `a06815037f927bbe1b0ed909ddd77e3eb4d2439c` added `scripts/validate-spiral-provenance.mjs`.
- `2006532235a6e6fb7366747026d6849827fcad4a` recorded `IMP-010`, the process-only implementation artifact for this graph repair and validator.
- `EVD-015` records mechanical traversal and strict-reference validation evidence.

Material implementation decisions or deviations from the initial likely work:

- The repair uses prospective current effective provenance links; it does not rewrite history and does not claim the original code was historically produced from links that were absent at the time.
- Current effective justification is encoded separately from implementation history. `sd:implements` carries the current effective design relation, while `sd:transforms`, `sd:changeCausedBy`, and verification evidence preserve historical/change reasons.
- The validator is dependency-free and repository-local. It validates the Turtle subset currently used by this repository rather than introducing a new package dependency or a full RDF toolchain.
- CYC-016 remains branched from CYC-015 because the requested traversal examples include the recent after-change/index characterization artifact.

Out-of-scope discoveries retained for later:

- This cycle did not change SimplyStore runtime behavior.
- This cycle did not add a full RDF/Turtle conformance dependency.
- This cycle did not attempt to repair every possible historical/process artifact path. Representative durability and after-change paths were the explicit target.
- Human prose is still needed to fully explain some current-effective versus historical-provenance judgments because the graph intentionally avoids collapsing multiple surviving reasons into one artificial linear cause.

## Evaluate

Integrated result against cycle goal:

The cycle goal is met for the representative paths required by the human request. Machine-readable traversal now reaches current effective intent from concrete implementation concerns through design, request, understanding, and retained human source using full strict-ancestor Git references.

Evidence / acceptance result:

`EVD-015` records the mechanical validation. The validator checks Turtle parsing for the repository subset, full hash form, Git reference resolution, strict ancestry, branch/cycle consistency, and representative traversals. `git diff --check` passes. Human acceptance is still pending.

Metric or risk movement:

The main risk moved from "the prose says why, but the graph cannot mechanically traverse to original human intent" to "representative durability and after-change implementation concerns have mechanically checked current-effective intent paths and separately recoverable history/evidence."

What changed in our understanding:

The graph can support the desired answer if current effective provenance and historical transition provenance stay separate. Trying to force one linear chain would be misleading for this project.

Surprises / model mismatches:

Uncommitted new Turtle artifacts cannot pass strict-ancestor validation because they do not yet have a source artifact commit. This is a useful operational consequence of the invariant: validate final graph state after semantic commits exist.

Known compromises:

- The validator is a targeted Spiral subset parser, not a general Turtle parser.
- The repair is representative rather than exhaustive.
- CYC-016 sits on top of the still-open CYC-015 branch ancestry until the human chooses how to integrate both cycles.

Unresolved issues within current goal:

No runtime/tooling blocker was found that requires SimplyStore product-code changes. Remaining expressiveness limits are documented in `EVD-015`.

Candidate next-cycle inputs:

- Decide whether to broaden the validator into a reusable Spiral tool or keep it repository-local.
- Decide how to merge/integrate CYC-015 and CYC-016 branch ancestry.
- Assess command timeouts as a distinct durability risk after this provenance repair cycle: especially command-worker timeout, handler hang, client/request timeout while processing continues, terminal status, replay, and unsafe classification.
- Resume the durability/product plan once this process repair is accepted.

Human evaluation / feedback:

Pending.

Cycle accepted, still open, or deliberately re-planned:

Open, pending human evaluation and acceptance.

## Process learning

What context/constraint/evaluation helped:

The strict-ancestor rule forced the repair into honest semantic commits, which made the causal-chain problem visible and testable instead of buried in prose.

What bookkeeping was useless:

None identified; the bookkeeping was directly relevant in this cycle because the bookkeeping was the system under test.

What should the environment learn from this cycle:

When repairing provenance, create semantic artifact versions before adding downstream historical references, and validate after commits exist. Treat current effective justification and historical transition cause as distinct graph questions.
