# Required integrity and existing-store initialization

Status: Accepted on 2026-09-23. Continue the roadmap durability/file-backed direction under
SRC-20260923-TTZ7C-52. The confirmed goal is mandatory artifact integrity,
explicit initialization for existing stores, and measured command latency.

Branch: spiral/CYC-20260923-TTZ7C-51-required-integrity.
Base exception: start from accepted CYC-44 tip `4680c76c8faabde53ecd712e1b903f2e8b0eddb6` because it supplies the
required file/index behavior and awaits separate integration authorization.
Do not merge it into master as part of this task. Preserve accepted history.

Scope: conversion, startup/loader, commands, recovery, stopped-store initializer,
example documentation, regression coverage and before/after latency evidence.
Keep trusted index loading; no automatic re-baselining, command replay during
initialization, data rewrite, version/audit feature, or new hash format.
Preserve the maintainer's example data and command. Test with disposable stores.

## Evaluation

Implementation is complete at `1e119dd101dd231a7cf24ba1b9d6c3e4999e8f60`. EVD-55 records 193 passing tests,
clean focused lint, successful initialization of a disposable copy of the actual
example with its command retained, and a 329 MB/48 MB-heap probe. Three alternating
30-command latency runs on local ext4 measured roughly 55 ms baseline versus
57 ms required-integrity median completion. There is one durable hash append
per artifact set. Human acceptance is recorded below; integration is pending.

The example now documents npm run init-integrity for a stopped existing store.
Actual user data/index/log files were preserved. Integration of accepted CYC-44
and this cycle is still a separate decision.

## Human acceptance

After testing the evaluated branch tip `9515993531c55176e1c3444f71dc3a23d5172df0`, the maintainer confirmed
on 2026-09-23:

> works as expected, fails to start without integrity now, init works, commands work.

This accepts the mandatory-integrity and existing-store initialization cycle.
The maintainer independently reports startup refusal without integrity, successful
initialization, and working commands. These are human-reported checks alongside
the 193 automated tests and latency evidence in EVD-20260923-TTZ7C-55.

The cycle is closed and ready to propose for integration with a normal merge
commit. This branch includes accepted CYC-44 and CYC-51. Integration remains a
separate decision; no next cycle is selected. The actual branch identity was
rechecked when recording acceptance.
