# Cycle: Conversion Offset Indexes

Branch: `spiral/CYC-20260917-TTZ7C-4-conversion-offsets`, created from clean
`master` and verified on 2026-09-17.

## Commitment And Evidence

After verification found 181,723 of 181,724 persisted offsets incorrect in the
local curriculum-store dataset, the maintainer clarified: "the offset index
code is incorrect, i want to correct that code, in simplystore first."
The ID index was correct. Inspection of SimplyStore's converter confirms that
index creation precedes the final serialization, although custom index hooks
can change record sizes. This request authorizes fixing that evidenced defect.

## Goal And Scope

Make conversion persist offsets matching the final OD-JSONTag output after
custom indexing. Preserve the CLI, custom create hook, and disk format. Verify
default conversion and hooks that grow, shrink, and add records, including
multibyte text. Do not change curriculum-store data, publish a package, or
redesign command-update index hooks in this cycle.

## Plan Continuity

Deliberate deviation at the maintainer's request from the durability/integrity
sequence in SRC-001, REQ-001, and DES-001. CYC-018 is accepted; this bounded
downstream-discovered correction does not replace that roadmap. The adopted
Muze engineering/library cultures apply; compatibility and simplicity are
explicit constraints. Evidence claims remain limited to conversion.

## Evaluation

Implemented final offset generation from the completed serialization. All 22
tests in `npm test` pass, including three conversion CLI regressions. The
readback/writer pass also produced exact byte ranges for all 181,724 records of
the downstream data file in a disposable directory, taking about five seconds;
the source file's SHA-256 remained unchanged. Test-file lint and diff checks
pass. EVD-20260917-TTZ7C-8 records the evidence and limits.

The code is ready for human evaluation on this branch. Human acceptance remains
pending; no merge, package release, or downstream-file repair was performed.
