# Cycle: Updated Roadmap And Planning Context

Status: Active; documentation work authorized by the completed roadmap interview.
Branch: spiral/CYC-20260919-TTZ7C-42-roadmap.
Base: b482053bdf0d3b3fc8902cce3169408d31ed9f6e (master).

Goal: update ROADMAP.md with the maintainer's clarified outcomes, preserve open
design decisions, and reconcile planning context so Spiral risk analysis selects
the actual next task rather than treating the roadmap order as a task queue.

Plan continuity: revise the earlier priority order following the completed
durability/recovery and runtime correction cycles. Keep durability invariants
and ordinary recovery restrictions. Full rebuild and CLI usability are the first
listed outcomes; broader testing is long range. No product cycle is selected.

Scope: retained interview source, roadmap, directly affected project context
and agent navigation, and documentation/provenance verification.

Non-goals: product code, dependencies, data, format changes, selecting a sandbox,
fixing merge semantics, general schema research, UI implementation or deployment.

Verification: compare the roadmap with each settled interview answer; preserve
revisions and deferred decisions; check local links, whitespace and causal
references. Runtime tests are unnecessary for documentation-only changes.

The actual branch was checked at creation. Recheck before semantic commits.
Existing untracked example indexes and package.json~ are outside this cycle.
Evaluation and human cycle acceptance precede a separate integration decision.

## Evaluation

Documentation is ready for human review; the cycle remains Active until accepted.
ROADMAP.md records the six agreed groups, separates initial outcomes from later
options, and delegates actual next-task selection to Spiral risk analysis.
The retained source records the interview's consequential corrections.

Review against the interview confirmed:

- Full-history rebuild precedes CLI usability in the listed resilience scope.
- File-backed-only retrieval preserves the remote API while allowing internal
  change; the maintainer's performance report is distinguished from new evidence.
- Versioning follows individual objects through deletion and reuse of the same ID.
- Workspace effects are prohibited; merge execution remains an investigation,
  with optional instance-level approval and an explicit account list.
- Public query access requires configurable authentication, read-only grants,
  restricted capabilities and enforced time/memory/output limits.
- UI audiences, login/landing/menu behavior, overridable exploration, schema
  authoring through UI or files, command documentation, personal fixtures and
  bring-your-own-AI conversation are retained.
- IDE, shared fixtures, authoring AI, MCP, broader resilience and automation
  remain later options.

Project context and AGENTS.md now lead future planning to the current roadmap.
The context recognizes the integrated runtime correction, the implemented
recovery baseline and the historical nature of older risk snapshots.

Verification: local Markdown links, whitespace and code fences passed review;
staged provenance validation passed with 107 documents and 189 references.
The dedicated branch identity was verified before each semantic commit.
Runtime tests were not run because only documentation and its graph changed.
No next product task, merge execution design or sandbox choice was selected.
