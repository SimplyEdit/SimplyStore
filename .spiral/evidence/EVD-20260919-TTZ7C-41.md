# Evidence: Runtime Contract Corrections

## Baseline before product changes

Test additions against the unchanged runtime at 665a97d reproduce all three
review findings. Running node --test --test-reporter=tap test/store-runtime.mjs
produces six passes (existing tests) and six failures (new tests):

- overlapping close resolves before the active command finishes;
- overlapping close reports success despite ownership-release failure;
- storage failure at each of two acceptance boundaries still returns success;
- a known ID-only retry returns 422 rather than its existing status;
- SIGTERM followed by SIGINT exits with command status active, rather than done.

The process test holds execution on an explicit release file, observes both
signals, then checks status and ownership and restarts the store. Direct tests
use controlled promises and real temporary log files. No product file changed
before this baseline run. The full pre-cycle review suite passed 112 tests;
these failures demonstrate its missing interleaving/compatibility coverage.

## Final implementation verification

Implementation at 5d9e81218d5a51d8d3171f7132e73c2e4bfc298a passes all 118 tests: the existing 112 plus
six new runtime contract cases. No failures, skips or TODOs. Command:

```sh
node --test --test-reporter=tap test/store-runtime.mjs \
  test/durability-startup.mjs test/conversion-offsets.mjs \
  test/command-offsets.mjs test/acid-baseline.mjs \
  test/durability-crash.mjs test/after-change-handlers.mjs \
  test/power-loss-baseline.mjs test/storage.mjs test/admin-recovery.mjs
```

Local-server suites require socket access outside this environment's restricted
sandbox; the run was authorized and used disposable stores. Focused ESLint passes
for the three modified production modules and test/store-runtime.mjs. Staged
provenance and whitespace validation passed before every semantic commit.

| Criterion | Result |
| --- | --- |
| R1 Closure | Controlled active execution blocks both close callers. Both observe cleanup rejection; repeated close retains that failure. The real process receives SIGTERM and SIGINT before its command is released, then records done, releases ownership and restarts with the committed value. |
| R2 Acceptance | Injected storage failure after log persistence or after accepted-status persistence rejects success and leaves the queue empty. Logged command evidence remains; subsequent admission is refused and ownership retained. |
| R3 Compatibility | A known ID-only retry returns accepted or done status without a second append/execution. An unknown ID without a name still returns 422. |
| R4 Readability | inspect() now lists inspection phases; prefix bookkeeping lives in reconstruction. runApprovedCommand() delegates activation numbering, audit and status serialization to markCommandActive(). No workflow order or report shape changed. |
| R5 Preservation | Full startup, offset, ACID, crash, handler, power-loss and administrative suites pass. |
| R6 Review boundary | The prior AGENTS.md edit is committed on the new cycle branch. Cycle acceptance and integration remain separate; master is unchanged. |

Production diff: runtime +8, inspection +4, recovery +6 net lines. Three
supporting methods clarify existing concerns; no new classes or dependencies.
The new tests add 209 net lines, including controlled interleavings and the
subprocess/restart proof. The failing baseline remains in Git at 959e2cd92181be07f3fe38d38328f10ba41923b3.

## Evaluation limits

This is evidence for the specified lifecycle contracts and preservation of the
covered behaviors, not a new filesystem/hardware durability claim. Randomized
and destructive soak work remains deferred under the governing roadmap.
Human cycle acceptance is pending. Integration is not authorized by acceptance
alone.
