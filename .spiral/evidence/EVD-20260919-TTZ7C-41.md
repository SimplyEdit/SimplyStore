# Evidence: Runtime Contract Corrections

## Baseline before product changes

Test additions against the unchanged runtime at 665a97d reproduce all three
review findings. Running node --test --test-reporter=tap test/store-runtime.mjs
produces six passes (existing tests) and six failures (new tests):

- overlapping close resolves before the active command finishes;
- overlapping close reports success despite ownership-release failure;
- storage failure at each of two acceptance boundaries still returns success;
- a known ID-only retry returns 422 rather than its existing status;
- SIGTERM followed by SIGINT exits with command status active, rather than done.

The process test holds execution on an explicit release file, observes both
signals, then checks status and ownership and restarts the store. Direct tests
use controlled promises and real temporary log files. No product file changed
before this baseline run. The full pre-cycle review suite passed 112 tests;
these failures demonstrate its missing interleaving/compatibility coverage.

Final implementation verification and human evaluation remain pending.
