---
id: EVD-20260916-TTZ7C-3
---

# Verification: Spiral Compliance Correction

## Claim and implementation under test

The process implementation at `adccdd13f4de9c0501a5f6e0c10a2778aa2b0eb8`
realizes `DES-20260916-TTZ7C-2`: exact references and lineage are corrected
prospectively, and historical defects are detected without rewriting history.
`IMP-010` governs the validator; the other verified implementation projections
record metadata corrections and CI adoption. This evidence makes no new claim
about SimplyStore runtime behavior.

Pinned process: `8d4b2c738a413abd4cccca740ce958f486e5f7af`.
Local integration target: `master` at `9b3bac5541b84420999fbadefeffc3be153e1d17`.
Branch checked: `spiral/CYC-20260819-09ZEF-5-integrity-detection`.

## Checks and observed results — 2026-09-16

Python used the existing `/tmp/simplystore-spiral-venv` environment with rdflib
7.6.0. That environment is local tooling, not a runtime dependency.

| Check | Result |
|---|---|
| `python test/spiral-provenance.py` | 16 regression tests passed. |
| Node entry point / committed snapshot | 66 Turtle documents, 85 references, no errors. |
| `python scripts/validate-spiral-provenance.py --range master..HEAD` | 10 commits, 22 changed Turtle versions, 41 references, no errors. |
| Staged validation before the implementation commit | 66 documents, 85 references, no errors. |
| `PYTHON=<venv-python> scripts/hooks/pre-commit` | Passed; the optional hook was exercised directly without replacing local hook configuration. |
| Pinned core validation of implementation tree | 66 Turtle files, 744 triples, no errors. |
| Original snapshot `fa329453bf640fab6083e39d57edfdbde222e282` | Failed with exactly the seven known `missing-artifact` errors. |
| `git diff --check` | Passed. |
| `git diff fa329453bf640fab6083e39d57edfdbde222e282 HEAD -- src package.json` | Empty: no runtime source or package changes. |

The regression histories cover malformed Turtle, custom relation/prefix syntax,
short hashes, missing target versions/paths, unavailable commits, sibling-branch
references, multiple reference targets, staged versus unstaged content, merges,
external references against source-version core pins, and invalid intermediate
versions later repaired or deleted. The raw-history fixture continues to fail
after a later repair; no historical suppression mechanism exists.

## Integration boundary

The pinned branch-aware integration validator against the local target rejects
the real candidate with `open-cycle-integration`. This is expected: the correction
reopened the cycle and has not received human acceptance.

A disposable clone used the same implementation tree with only the cycle status
changed to Accepted, in hypothetical commit
`fbd2f448a9f8e946bc1b048a005f0ec251722f00`. Integration validation passed there
(66 Turtle files, 744 triples). This is a diagnostic fixture only, not acceptance,
a proposed real commit, or a reason to bypass the real integration gate.

## Limits

- Human acceptance of the correction is pending. The runtime slice's earlier
  acceptance remains historical evidence.
- Remote branch protection and hosted CI execution were not verified. The local
  target may need refreshing before actual integration.
- The existing invalid historical versions remain invalid. This is not a claim
  that all reachable history is pristine or has been fully audited.
- Runtime test suites were not rerun because runtime code and dependencies did
  not change; the earlier runtime test results remain historical claims.
- Lineage ownership still needs human/agent review; the checker cannot infer it
  merely from overlapping source paths.
- This evidence was added after testing the implementation commit; its own
  references are validated before the evidence commit and again at the final tip.
